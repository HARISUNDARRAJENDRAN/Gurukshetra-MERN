# configs

This folder is an alias-friendly structure for configuration modules.

Primary config implementations currently live in `backend/config/`:
- `mongodb.js`
- `nodemailer.js`
- `cloudinary.js`

Use these config modules to initialize backend services.
