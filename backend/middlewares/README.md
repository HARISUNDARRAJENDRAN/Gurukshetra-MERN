# middlewares

This folder is an alias-friendly structure for middleware modules.

Primary middleware implementations currently live in `backend/middleware/`:
- `userAuth.js`

You can add shared middlewares here as the API surface grows.
